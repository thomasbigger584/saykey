# Saykey ASR server -- base dependencies.
# onnxruntime / onnxruntime-gpu is installed separately (scripts\install.ps1
# picks the right one based on detected GPU) so this file works for both.

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
numpy>=1.24
soundfile>=0.12.1
onnx-asr[hub]>=0.5.0
faster-whisper>=1.0.0
