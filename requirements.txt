# VDI Dictate -- offline speech-to-text runtime.
# CPU-only. No PyTorch and no ffmpeg required: faster-whisper bundles
# CTranslate2 + PyAV, and audio is fed in as a raw numpy array.

faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24
