# Saykey desktop UI (cross-platform: Windows / macOS / Linux).
# Install into the same .venv as requirements.txt:
#     .venv\Scripts\python -m pip install -r requirements-ui.txt

PySide6>=6.6
sounddevice>=0.4.6
pynput>=1.7          # cross-platform hotkey capture; seed of the future native agent
